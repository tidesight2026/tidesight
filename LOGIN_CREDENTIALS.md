# 🔐 معلومات تسجيل الدخول - AquaERP

## ✅ تم إعداد المستخدم بنجاح!

### معلومات تسجيل الدخول

**Tenant:** Farm 1  
**Domain:** farm1.localhost  
**URL:** http://farm1.localhost:8000/admin/

#### بيانات المستخدم:
- **Username:** `SmartFarm`
- **Email:** `admin@farm1.com`
- **Password:** `admin123`

## 🚀 خطوات تسجيل الدخول

1. **تأكد من إضافة hosts** (إذا لم تكن أضفتها):
   - افتح `C:\Windows\System32\drivers\etc\hosts` كمسؤول
   - أضف: `127.0.0.1    farm1.localhost`
   - احفظ الملف

2. **افتح المتصفح وانتقل إلى:**
   - http://farm1.localhost:8000/admin/

3. **أدخل بيانات تسجيل الدخول:**
   - Username: `SmartFarm`
   - Password: `admin123`

4. **اضغط "ادخل" (Log in)**

## 🔧 تغيير كلمة المرور

إذا أردت تغيير كلمة المرور:

```bash
docker-compose exec web python create_admin_user.py
```

ثم عدّل الملف `create_admin_user.py` وغير قيمة `PASSWORD`.

## 📋 إنشاء مستخدم جديد

لإنشاء مستخدم admin جديد:

```bash
docker-compose exec web python manage.py create_tenant_superuser --schema_name farm1 --username <username> --email <email> --noinput
```

**ملاحظة:** هذا الأمر سينشئ مستخدم بدون كلمة مرور. استخدم `create_admin_user.py` لتحديد كلمة مرور.

## ✅ الحالة الحالية

- ✅ Tenant تم إنشاؤه (Farm 1)
- ✅ الترحيلات تم تشغيلها
- ✅ مستخدم Admin تم إنشاؤه/تحديثه
- ✅ كلمة المرور: `admin123`

**🎊 جاهز للاستخدام الآن!**
