# 🔴 تشخيص مشكلة 404 للـ API Endpoint

**التاريخ:** ديسمبر 2025  
**المشكلة:** `/api/auth/login` يعيد 404 Not Found عند الوصول عبر HTTP

---

## ✅ ما تم التحقق منه

### 1. المسار موجود ويعمل ✅
```
✅ /api/auth/login -> api-1.0.0:login
```

### 2. Tenant و Domain موجودان ✅
- Tenant: `farm1` (مزرعة تجريبية)
- Domain: `farm1.localhost`

### 3. CORS يعمل ✅
- OPTIONS request → 200 OK
- المشكلة في POST request فقط

### 4. Django Ninja API مُكوّن بشكل صحيح ✅
- Router موجود
- Endpoints موجودة

---

## 🔍 المشكلة

- ✅ المسار موجود ويتم التعرف عليه في الاختبار
- ❌ لكن عند الوصول عبر HTTP request، يحصل على 404

---

## 🧪 خطوات التشخيص الموصى بها

### 1. تجربة Postman

جرّب إرسال POST request مباشرة:

```http
POST http://farm1.localhost:8000/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "Admin123!"
}
```

**إذا حصلت على 404 أيضًا:**
- المشكلة في Backend (Django Ninja + django-tenants)
- **إذا عمل:**
- المشكلة في Frontend أو CORS

---

### 2. التحقق من hosts file

**Windows:**
```
C:\Windows\System32\drivers\etc\hosts
```

يجب أن يحتوي على:
```
127.0.0.1    farm1.localhost
```

---

### 3. فحص Network Tab في المتصفح

1. افتح Developer Tools (F12)
2. اذهب إلى **Network** tab
3. جرّب تسجيل الدخول
4. انقر على الطلب `/api/auth/login`
5. تحقق من:
   - **Request URL:** هل هو `http://farm1.localhost:8000/api/auth/login`؟
   - **Request Headers:** هل يحتوي على `Host: farm1.localhost:8000`؟
   - **Response:** ما هو الرد الفعلي؟

---

## 🔧 الحلول المحتملة

### الحل 1: التحقق من Django Ninja Configuration

قد تكون المشكلة في كيفية تكوين Django Ninja مع django-tenants.

### الحل 2: التحقق من Tenant Context

قد تكون المشكلة في أن Django Ninja لا يجد tenant context بشكل صحيح.

### الحل 3: التحقق من URL Routing

قد تكون المشكلة في كيفية توجيه URLs في django-tenants.

---

## 📋 البيانات اللازمة للتشخيص

بعد تجربة Postman، أرسل:

1. **Response Status Code:** (200, 404, 500, إلخ)
2. **Response Body:** محتوى الرد الكامل
3. **Request Headers:** جميع الـ headers المُرسلة
4. **Backend Logs:** آخر 20 سطر من `docker-compose logs web`

---

## ✅ الخطوة التالية

**جرّب Postman أولاً ثم أرسل النتائج!** 🚀

