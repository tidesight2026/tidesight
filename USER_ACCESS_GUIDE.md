# دليل وصول المستخدمين إلى الشاشة الرئيسية - AquaERP

## 📋 نظرة عامة

بعد إنشاء المستخدمين وتوزيع الأدوار داخل Tenant، يمكن للمستخدمين الوصول إلى الشاشة الرئيسية (Dashboard) لإجراء المعاملات اليومية.

---

## 🚀 الخطوات الكاملة

### 1. تشغيل Frontend

**في التطوير:**
```bash
cd frontend
npm run dev
```

Frontend سيعمل على: `http://localhost:5175`

**في الإنتاج:**
Frontend يعمل عبر Docker على المنفذ 80.

---

### 2. الوصول إلى صفحة تسجيل الدخول

افتح المتصفح وانتقل إلى:
```
http://localhost:5175/login
```

أو إذا كنت تستخدم tenant domain:
```
http://farm1.localhost:5175/login
```

---

### 3. تسجيل الدخول

**بيانات تسجيل الدخول:**
- **Username:** اسم المستخدم الذي تم إنشاؤه (مثل: `admin`, `manager`, `accountant`)
- **Password:** كلمة المرور التي تم تعيينها

**مثال:**
- Username: `admin`
- Password: `admin123`

---

### 4. الوصول التلقائي إلى Dashboard

بعد تسجيل الدخول بنجاح، سيتم توجيهك تلقائياً إلى:
```
http://localhost:5175/dashboard
```

هذه هي **الشاشة الرئيسية** حيث يمكنك:
- عرض الإحصائيات
- الوصول إلى جميع الوحدات
- إجراء المعاملات اليومية

---

## 🔧 إعداد API URL لكل Tenant

### المشكلة الحالية

`API_BASE_URL` في `frontend/src/utils/constants.ts` مضبوط على:
```typescript
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://farm1.localhost:8000'
```

### الحل: استخدام متغير البيئة

**للتطوير:**
أنشئ ملف `.env` في مجلد `frontend/`:
```env
VITE_API_BASE_URL=http://farm1.localhost:8000
```

**لـ tenant آخر (مثل tmco):**
```env
VITE_API_BASE_URL=http://tmco.localhost:8000
```

---

## 📱 الوصول لكل Tenant

### Tenant 1: Farm 1
```
Frontend: http://farm1.localhost:5175/login
Backend:  http://farm1.localhost:8000/api
```

### Tenant 2: Tilapia Marine Company
```
Frontend: http://tmco.localhost:5175/login
Backend:  http://tmco.localhost:8000/api
```

**ملاحظة:** يجب إضافة Domains إلى `hosts` file:
```
127.0.0.1    farm1.localhost
127.0.0.1    tmco.localhost
```

---

## 🎯 الصفحات المتاحة بعد تسجيل الدخول

### 1. Dashboard (الشاشة الرئيسية)
- **URL:** `/dashboard`
- **الوصول:** تلقائي بعد تسجيل الدخول
- **المحتوى:** إحصائيات عامة، رسوم بيانية، ملخصات

### 2. Farm (المزرعة)
- **URL:** `/farm`
- **المحتوى:** معلومات المزرعة، الإعدادات

### 3. Ponds (الأحواض)
- **URL:** `/ponds`
- **المحتوى:** إدارة الأحواض

### 4. Batches (الدفعات)
- **URL:** `/batches`
- **المحتوى:** إدارة دفعات الأسماك

### 5. Inventory (المخزون)
- **URL:** `/inventory`
- **المحتوى:** إدارة المخزون (علف، أدوية)

### 6. Daily Operations (العمليات اليومية)
- **URL:** `/operations`
- **المحتوى:** تسجيل التغذية، الوفيات، الفحوصات

### 7. Accounting (المحاسبة)
- **URL:** `/accounting`
- **المحتوى:** القيود المحاسبية، التقارير المالية
- **الصلاحيات:** يتطلب صلاحية `accounting`

### 8. Reports (التقارير)
- **URL:** `/reports`
- **المحتوى:** تقارير الأداء، الربحية
- **الصلاحيات:** يتطلب صلاحية `reports`

### 9. Sales (المبيعات)
- **URL:** `/sales`
- **المحتوى:** طلبات البيع، الفواتير

### 10. Settings (الإعدادات)
- **URL:** `/settings`
- **المحتوى:** إعدادات المستخدم، اللغة

---

## 🔐 الصلاحيات والأدوار

### الأدوار المتاحة:
- **Owner (المالك):** صلاحيات كاملة
- **Manager (المدير):** إدارة يومية
- **Accountant (محاسب):** صلاحيات محاسبية
- **Worker (عامل):** عمليات يومية فقط
- **Viewer (مشاهد):** عرض فقط

### الصلاحيات المميزة:
- **accounting:** الوصول إلى المحاسبة
- **reports:** الوصول إلى التقارير
- **audit:** الوصول إلى سجلات التدقيق

---

## 🐛 حل المشاكل الشائعة

### 1. خطأ "Network Error" عند تسجيل الدخول

**السبب:** `API_BASE_URL` غير صحيح

**الحل:**
1. تحقق من `VITE_API_BASE_URL` في `.env`
2. تأكد من أن Backend يعمل على المنفذ 8000
3. تأكد من أن Tenant domain موجود في `hosts` file

### 2. خطأ "401 Unauthorized"

**السبب:** بيانات تسجيل الدخول غير صحيحة

**الحل:**
1. تحقق من Username و Password
2. تأكد من أن المستخدم موجود في Tenant الصحيح
3. تأكد من أن المستخدم `is_active = True`

### 3. خطأ "403 Forbidden" عند الوصول لصفحة

**السبب:** المستخدم لا يملك الصلاحيات المطلوبة

**الحل:**
1. تحقق من دور المستخدم
2. تحقق من الصلاحيات المطلوبة للصفحة
3. تحديث دور المستخدم من Admin

### 4. Frontend لا يعمل

**الحل:**
```bash
cd frontend
npm install
npm run dev
```

---

## 📝 مثال كامل: تسجيل دخول مستخدم جديد

### 1. إنشاء مستخدم من Django Admin

1. افتح: `http://farm1.localhost:8000/admin/`
2. اذهب إلى: **Account Management > Users**
3. انقر: **+ Add User**
4. املأ البيانات:
   - Username: `manager1`
   - Password: `manager123`
   - Email: `manager1@farm1.com`
   - Role: `manager`
   - Is Active: ✓
5. احفظ

### 2. تسجيل الدخول من Frontend

1. افتح: `http://farm1.localhost:5175/login`
2. أدخل:
   - Username: `manager1`
   - Password: `manager123`
3. انقر: **تسجيل الدخول**
4. سيتم توجيهك تلقائياً إلى `/dashboard`

### 3. الوصول إلى الوحدات

من Dashboard، يمكنك:
- النقر على **الأحواض** للوصول إلى `/ponds`
- النقر على **الدفعات** للوصول إلى `/batches`
- النقر على **العمليات اليومية** للوصول إلى `/operations`

---

## 🎉 النتيجة

بعد تسجيل الدخول، المستخدم يمكنه:
- ✅ الوصول إلى Dashboard (الشاشة الرئيسية)
- ✅ إجراء المعاملات اليومية
- ✅ الوصول إلى جميع الوحدات حسب الصلاحيات
- ✅ عرض التقارير والإحصائيات

---

**تاريخ الإنشاء:** 2025-12-12
