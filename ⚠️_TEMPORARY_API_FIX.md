# ⚠️ إصلاح مؤقت: نقل API إلى SHARED_APPS

**التاريخ:** ديسمبر 2025  
**الحالة:** حل تجريبي  
**الهدف:** حل مشكلة 404 للـ API endpoints

---

## 🔧 التعديل المطبق

تم نقل `api` من `TENANT_APPS` إلى `SHARED_APPS` مؤقتاً:

### في `tenants/aqua_core/settings.py`

**قبل:**

```python
TENANT_APPS = (
    ...
    'api',  # API endpoints - معزول لكل tenant
)
```

**بعد:**

```python
SHARED_APPS = (
    ...
    'api',  # ⚠️ TEMPORARY: نقل مؤقتاً لحل مشكلة 404
)

TENANT_APPS = (
    ...
    # 'api',  # تم نقله إلى SHARED_APPS
)
```

---

## ⚠️ تحذيرات مهمة

1. **هذا حل مؤقت:** الهدف هو حل مشكلة 404 أولاً، ثم البحث عن حل أفضل
2. **ليس مثالي:** `api` يجب أن يكون في `TENANT_APPS` لضمان العزل
3. **يحتاج مراجعة:** بعد حل المشكلة، يجب البحث عن حل صحيح

---

## ✅ الاختبار

بعد إعادة تشغيل Backend:

1. **اختبار Swagger UI:**

   ```
   http://farm1.localhost:8000/api/docs
   ```

2. **اختبار Login:**

   ```
   POST http://farm1.localhost:8000/api/auth/login
   ```

---

## 🔄 الخطوات التالية

إذا عمل الحل:

- ✅ متابعة Sprint 1
- ⏳ البحث عن حل أفضل لاحقاً
- ⏳ إعادة `api` إلى `TENANT_APPS` مع الحل الصحيح

إذا لم يعمل:

- 🔍 البحث عن حلول بديلة
- 🔍 فحص middleware و tenant context

---

**ملاحظة:** هذا حل تجريبي فقط! 🚀
