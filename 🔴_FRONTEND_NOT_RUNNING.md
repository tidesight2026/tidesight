# 🔴 Frontend لا يعمل - AquaERP

**التاريخ:** ديسمبر 2025

---

## ⚠️ المشكلة

خطأ `ERR_CONNECTION_REFUSED` يعني أن Frontend **لا يعمل** على المنفذ `5174`.

---

## ✅ الحل السريع

### الطريقة 1: تشغيل يدوي (الأفضل)

1. **افتح Terminal جديد** (لا تغلق الحالي)

2. **انتقل إلى مجلد Frontend:**
   ```powershell
   cd D:\AquaERP\frontend
   ```

3. **شغّل Frontend:**
   ```powershell
   npm run dev
   ```

4. **انتظر حتى ترى:**
   ```
   VITE v5.x.x  ready in xxx ms
   
   ➜  Local:   http://localhost:5174/
   ```

5. **افتح المتصفح:**
   ```
   http://localhost:5174
   ```

---

## 🔍 التحقق من المنفذ

### إذا كان المنفذ مشغولاً:

```powershell
netstat -ano | findstr :5174
```

إذا وجدت عملية، يمكنك:
- إيقاف العملية
- أو استخدام منفذ آخر

---

## 🚀 الخطوات الكاملة

### 1. افتح Terminal جديد

افتح PowerShell جديد (لا تغلق Terminal الحالي)

### 2. انتقل إلى المجلد

```powershell
cd D:\AquaERP\frontend
```

### 3. شغّل Frontend

```powershell
npm run dev
```

### 4. افتح المتصفح

افتح: `http://localhost:5174`

---

## 🔑 بيانات تسجيل الدخول

```
Username: admin
Password: Admin123!
```

---

## ✅ قائمة التحقق

- [ ] فتح Terminal جديد
- [ ] الانتقال إلى `D:\AquaERP\frontend`
- [ ] تشغيل `npm run dev`
- [ ] انتظار رسالة "ready"
- [ ] فتح `http://localhost:5174`

---

## 🎯 النتيجة المتوقعة

بعد تشغيل `npm run dev`، يجب أن ترى:

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5174/
➜  Network: use --host to expose
```

---

**شغّل Frontend في Terminal جديد الآن!**

---

**تاريخ الإنشاء:** ديسمبر 2025

