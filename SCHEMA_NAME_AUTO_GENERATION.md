# توليد Schema Name تلقائياً - AquaERP

## ✅ ما تم إنجازه

تم إضافة method `save_model` في `ClientAdmin` لتوليد `schema_name` تلقائياً من اسم الشركة إذا لم يكن محدداً.

## 🔧 كيف يعمل

### 1. التحويل التلقائي
- يتم تحويل اسم الشركة إلى `schema_name` صالح
- إزالة الأحرف العربية والخاصة
- تحويل إلى lowercase
- استبدال المسافات والأحرف الخاصة بـ underscore

### 2. أمثلة التحويل

| اسم الشركة | schema_name |
|------------|-------------|
| `Tilapia Marine Company` | `tilapia_marine_company` |
| `شركة الأسماك المحدودة` | `tenant_1` (إذا لم يكن هناك نص إنجليزي) |
| `Farm 2` | `farm_2` |
| `ABC Company 123` | `abc_company_123` |

### 3. التحقق من التكرار
- إذا كان `schema_name` موجود بالفعل، يتم إضافة رقم متسلسل
- مثال: `tilapia_marine_company`, `tilapia_marine_company_1`, `tilapia_marine_company_2`

### 4. القيود
- الطول الأقصى: 63 حرف (PostgreSQL limit)
- يجب أن يبدأ بحرف أو underscore
- لا يمكن أن يبدأ برقم (يتم إضافة prefix `tenant_`)

## 📝 الاستخدام

### في Django Admin

1. **افتح صفحة إضافة عميل:**
   - http://localhost:8000/admin/tenants/client/add/

2. **املأ البيانات:**
   - **اسم الشركة:** `Tilapia Marine Company`
   - **البريد الإلكتروني:** `info@tilapia.com`
   - **الهاتف:** (اختياري)
   - **نوع الاشتراك:** اختر من القائمة
   - **Schema Name:** سيتم توليده تلقائياً (يمكنك تحديده يدوياً إذا أردت)

3. **احفظ:**
   - سيتم توليد `schema_name` تلقائياً: `tilapia_marine_company`

## ⚠️ ملاحظات

- **عند الإضافة:** يمكنك تحديد `schema_name` يدوياً أو تركه فارغاً ليتم توليده تلقائياً
- **عند التعديل:** `schema_name` يكون readonly ولا يمكن تغييره
- **التكرار:** إذا كان `schema_name` موجود، سيتم إضافة رقم متسلسل تلقائياً

---

**تاريخ الإكمال:** 2025-12-11
